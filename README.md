At least for now you can run the kitbuild script that will build a tarball
you can copy around, unextract and run INSTALL

./kitbuild
<br>
cd /tmp
<br>
tar -zxf python-gptools-0.0.8.src.tar.gz
<br>
cd python-gptools-0.0.7
<br>
sudo ./INSTALL
